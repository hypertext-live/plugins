name = "std::test"
version = "1.0.0"
description = "Library for testing plugin compatibility"
type = "partial"
keywords = ["Layout", "Partial"]

[dependencies."std::core"]
version = "~1"
optional = true

[dependencies."font::inter"]
version = "~3"
optional = true

[features]
inter = ["font::inter"]
reference = ["inter"]
default = ["std::core"]
